{
  "name": "marcoledesma-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "post:new": "node scripts/new-post.js",
    "post:build": "npm run build",
    "post:publish": "npm run build && git add -A && git commit -m 'chore: publish site' && git push"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
